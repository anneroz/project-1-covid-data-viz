

// let series = Object.values(groups).map( (group, month) => {
//     return {
//         'x': monthNames[month],
//         'y': group.reduce((acc, datnum) => acc + datnum.vaccines, 0)
//     }
// });

// console.log(series);